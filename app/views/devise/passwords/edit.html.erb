<div class="heading text-center">
  <span>Password edit</span>
</div>

<div class="password_reset_description">
  <p class="text-center">新しいパスワードをフォームに入力し、<br>
    パスワード更新を完了させてください。</p>
</div>

<div class="password_reset_form">

  <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put }) do |f| %>
    <%= render "devise/shared/error_messages", resource: resource %>
    <%= f.hidden_field :reset_password_token %>

    <div class="form-group">
      <%= f.password_field :password, autofocus: true, autocomplete: "new-password",
      :placeholder => "新しいパスワード",id: "new_password", class: "form-control"%>
    </div>

    <div class="form-group">
      <%= f.password_field :password_confirmation, autocomplete: "new-password",
      :placeholder => "確認のため再入力してください",id: "new_password_confirmation",class: "form-control"%>
    </div>

    <div class="text-right">
      <%= f.submit "Submit", class: "submit-button"%>
    </div>
  <% end %>

</div>




