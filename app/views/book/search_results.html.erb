<div class="heading text-center">
  <span>Search</span>
</div>

<div class="search">

<div class="search-box text-center">
  <p>Giveされている本を検索する</p>
  <%= form_tag(book_search_results_path,:method => 'get') do %>
    <%= text_field_tag :search %>
    <%= submit_tag 'Search', :name => nil %>
  <% end %>
</div>


  <div class="grid">
    <% @books.each do |book| %>
    <div class="book-cover text-center">
<!--      画像のところはそのまま本の書影がくる-->
      <%= image_tag "zikken.png" %>
      <p class="search-book-title"><%= book.name %></p>
      <p><%= book.giver.name %>さんがGiveしています</p>
    </div>;
    <% end %>
  </div>

  <div class="text-center pagination">
    <%= paginate @books %>
  </div>


</div>







<%= render "layouts/exhibit_fix_button" %>